# Use a Node.js base image for the frontend
FROM node:18-alpine

# Set environment variables for Next.js and dev server
ENV NODE_ENV=development
ENV PORT=3000

# Set the working directory
WORKDIR /app

# Copy the package files
COPY package.json ./

# Install dependencies
RUN npm install

# Copy the rest of the application code
COPY . .

# Expose the port used by Next.js/React
EXPOSE 3000

# Command to start the Next.js development server
CMD ["npm", "run", "dev"]